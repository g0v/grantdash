extends layout

block styles

  link(rel='stylesheet', href='/styles/landing.css?v=' + version)

block content

block scripts

  script(src="/js/hackdashApp.js?v=" + version)

  script.
    $(function(){
      window.hackdash.startApp();
      if (!window.hackdash.user && document.location.pathname === '/login') {
        hackdash.app.showLogin();
      }
    });
